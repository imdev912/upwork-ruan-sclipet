import { InfoBanner } from "../InfoBanner";

export const Header = () => {
    return (
        <header>
            <InfoBanner>
                <>
                    {/* header message */}
                    ATENÇÃO: UMA OPORTUNIDADE ÚNICA SERÁ REVELAA NO FINAL DO VÍDEO!
                </>
            </InfoBanner>
        </header>
    );
}