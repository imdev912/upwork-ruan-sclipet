import { InfoBanner } from "../InfoBanner";

export const Header = () => {
    return (
        <header>
            <InfoBanner>
                <>
                    {/* header message */}
                    ATENÇÃO: UMA OPORTUNIDADE ÚNICA SERÁ REVELADA NO FINAL DO VÍDEO!
                </>
            </InfoBanner>
        </header>
    );
}